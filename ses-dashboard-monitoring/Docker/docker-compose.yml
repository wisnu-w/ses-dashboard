services :
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: ses_user
      POSTGRES_PASSWORD: password123!
      POSTGRES_DB: ses_dashboard
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata: